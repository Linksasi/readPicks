{
  "name": "readpicks-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "拾词 Android 壳（Capacitor）：webDir=www 与手机浏览器版同一份代码",
  "scripts": {
    "cap": "cap",
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  }
}
